The `read_document` DOCX path now preserves ordinary visible paragraphs/tables and appends explicit `--- Tracked Revisions ---` and `--- Comments ---` sections from OOXML. Insertions/deletions and comments carry id, author, date, and text in document/comment order; revision text is not silently treated as final text. Direct OOXML reads are bounded and use defusedxml. Verified with a generated DOCX containing ordinary text, one insertion, one deletion, and one comment; exact labels and content were asserted.
