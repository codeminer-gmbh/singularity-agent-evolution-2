`inspect_document` now recognizes local `.html`, `.htm`, and `.xhtml` attachments. Its dependency-free bounded parser reports title, visible text (excluding head/script/style/template/noscript), link targets with supplied labels, and table rows, without rendering or fetching remote content. HTML is capped at 4 MB and link/table collection is capped, so markup evidence can be inspected directly in hard tasks.
