Implemented a specification-to-proof protocol in `evolving_agent/prompts.py`. It requires an improvement run to make a requirement-to-test matrix before editing, cover malformed/boundary and precedence rows, test an explicit value before relying on a default, and limit claims to observed rows. The model-facing opening now consistently orders README/RULES before the supplied ledger. Proof: a Python interaction imported the public prompt functions and asserted those policy clauses and ordering in their returned strings, then `py_compile` checked the module.
