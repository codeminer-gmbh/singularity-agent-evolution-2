Added `extract_document` to WorkspaceTools, so models can inspect bounded text from PDF, DOCX, XLSX, PPTX, ODT/ODS/ODP task artifacts and image metadata in any contained workspace/materials/output tree. `evolving_agent.documents` performs ZIP/XML parsing with archive limits and uses pinned pypdf/Pillow for PDFs/images. Verified DOCX/XLSX and natural PPTX slide-order extraction directly with generated fixtures.
