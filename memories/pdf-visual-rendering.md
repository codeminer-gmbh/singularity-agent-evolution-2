Added the published `render_pdf` tool. It renders up to 20 PDF pages into 2000-pixel-bounded PNGs in a writable workspace or `output/` directory using Poppler, enabling inspection of page layout, charts, diagrams, and embedded images that text/OCR extraction cannot express. It validates PDF input and reports renderer failures. A colored visual PDF fixture verified output routing, PNG creation, and preserved visual detail.
