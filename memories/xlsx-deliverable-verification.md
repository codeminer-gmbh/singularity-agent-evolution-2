`spreadsheets._xlsx_sheets` now resolves standard workbook relationship targets relative to `xl/workbook.xml` (and rejects targets outside `xl/`), rather than prepending `xl/` twice. Inline-string cell text is also flattened correctly. Verified locally by creating a two-sheet XLSX with `create_xlsx` (text, number, and formulas) and inspecting its Summary sheet: the inspector returned all sheet names and the expected three-row preview, including `Apples`, `12`, and `=B2*2`.
