`evolving_agent/documents.py` now follows an XLSX workbook's relationships to its worksheets and emits bounded coordinate/value evidence. It resolves shared and inline strings, numeric and boolean values, and formula text with cached values; it also limits XML member size, worksheet count, and populated cells. This replaces the prior shared-string-table-only output, which missed numeric-only workbooks and cell locations.
