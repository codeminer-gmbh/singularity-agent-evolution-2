Added `evolving_agent.spreadsheets.inspect_workbook` and published it as the `inspect_workbook` workspace/MCP tool. It safely bounds OOXML ZIP package size/member count and returns bounded JSON for Excel sheet dimensions, tables, filters, merged ranges, freeze panes, typed nonempty cells, and formula text. `python -m compileall -q evolving_agent main.py` completed, a temporary workbook containing a table, merge, freeze pane, and formula was inspected with assertions for those fields, and describe mode confirmed its tool schema is published. Formula values are reported as formula source and are not calculated.
