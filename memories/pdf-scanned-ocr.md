`inspect_pdf` now accepts optional `ocr: true`: it renders no more than five selected pages in a private temporary directory with bounded Poppler resolution and extracts English text through Tesseract. OCR output is separately bounded per page, records whether selected pages exceeded the OCR cap, and preserves default native-text-only inspection. The Docker image installs `poppler-utils`; converter processes have timeouts and no shell invocation.
