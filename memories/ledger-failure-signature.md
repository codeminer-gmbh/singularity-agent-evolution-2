The improvement role now requires a ledger verdict to be translated into an observable failure signature and a distinguishing matrix input before design selection. For ordering, precedence, defaults, and strict parsing it additionally demands an explicit decision table, including declaration-versus-encounter ordering, so a nearby happy-path test cannot substitute for the reported failure.