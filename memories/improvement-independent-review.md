Improvement mode now uses two mandatory tool-capable completion stages: an adversarial, ledger-conditioned reviewer independently runs the most distinguishing matrix interaction, then a finalizer repairs any defect, reruns that exact interaction, and refreshes `memories/verification.json`. The public-path regression test scripts a false precedence claim, observes the reviewer expose `first` versus expected `last`, and verifies the finalizer repairs and records `last`.