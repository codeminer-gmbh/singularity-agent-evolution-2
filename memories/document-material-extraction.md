Added the `extract_document_text` workspace tool for read-only or workspace PDF, DOCX, XLSX, and PPTX inputs. It emits page/sheet/slide labels, DOCX table rows, spreadsheet text, and bounded output; XLSX extraction additionally preserves omitted-column positions from A1 references and exposes a formula when a generator supplied no cached value. Representative checks created temporary materials and verified DOCX (`Decision memo`, table row), XLSX, PPTX, and PDF extraction; the sparse/formula XLSX check observed `Product |  | Total` and `Widget |  | =SUM(B2:B9)` through the registered tool.