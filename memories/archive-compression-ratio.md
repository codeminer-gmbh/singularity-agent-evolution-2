Archive extraction now rejects entries whose declared (ZIP) or streamed (TAR) expansion exceeds a 1,000:1 compressed-input ratio, in addition to its existing member and total-byte limits. `_CountingReader` is passed directly into streaming `tarfile` parsing so the TAR extraction loop compares actual outer bytes consumed as it writes each member. Verified with normal ZIP and gzip-TAR extraction plus highly compressed ZIP and gzip-TAR payload rejection.
