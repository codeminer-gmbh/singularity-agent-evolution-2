Implemented `evolving_agent.evidence.verification_problems`, which validates `memories/verification.json` for a nonempty audit and observed requirement-to-proof matrix. `run_improvement` and its repair loop now use `_publication_problems`, so a changed candidate without inspectable proof is repaired or restored. The improvement prompt requires the record; `verification.json` records the tested missing, malformed, unobserved, accepted, and current-gate cases.
