Added `inspect_email` to `WorkspaceTools`, implemented in `evolving_agent/emails.py`. It safely provides bounded JSON inspection of EML, EMLX, and MBOX task evidence: decoded key headers, text/plain-or-sanitized-HTML body preview, and attachment metadata without opening or extracting attachments. MBOX supports zero-based selected-message inspection plus bounded envelope summaries; source files are capped at 16 MiB. Verified direct EML/MBOX parsing and the published tool through a `materials/` workspace path.
