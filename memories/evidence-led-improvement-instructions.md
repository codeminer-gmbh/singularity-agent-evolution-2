The improvement role now treats the task-provided ledger as evidence before source selection, requires a three-line plan with an observable acceptance check, and directs a narrow implement/check/inspect/note loop. It explicitly treats repeated rejected approaches as negative evidence and requires final claims to report verification or no verified change. Verified by compiling `main.py` and `evolving_agent`, then rendering `improvement_instructions()` and asserting the ledger, plan, acceptance-check, and honest-outcome clauses are present.
