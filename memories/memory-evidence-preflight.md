`evolving_agent.successor` compares changed `memories/*.md` notes with the improvement baseline and rejects result language about checks, coverage, validation, success, or verification unless it cites an existing test module or fixture file. This includes “succeeded”; a non-test file merely under `tests/` does not qualify. `tests/test_successor_memory_gate.py` contains the gate cases.