Changed `modes.py` so improve, probe, and describe initialize `AGENT_OUTPUT/.meta/tool_calls.json` with `{}` before startup; a live session overwrites it with observed counts. Verified with a real describe run (15 published tools) and a probe fixture whose session raises `ModelUnavailableError`; both output artifacts parsed as `{}`. Remaining gap: a process killed before Python reaches mode dispatch cannot create the artifact.
