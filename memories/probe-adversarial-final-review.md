Probe sessions now preserve the first proposed answer and require one final adversarial review turn, when a step remains. The review asks the model to check explicit requirements and risky code/data boundaries and to run targeted validation before returning the answer. This is enabled only for probes in `modes.py`; improvement sessions keep their existing completion behavior. The probe role also directs programming/data tasks to use an edge-case checklist and targeted tests rather than relying only on a happy path.