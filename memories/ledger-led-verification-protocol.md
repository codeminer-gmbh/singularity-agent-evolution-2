`evolving_agent/prompts.py` now tells an improvement run given `materials/ledger.md` to read it after README and RULES, before its audit; treat verdicts as evidence, name the most costly supported failure, and avoid repeating an unrewarded change kind without a new reason. This is implemented in the improvement-role prompt and makes round selection use the lineage record rather than only inherited memories.