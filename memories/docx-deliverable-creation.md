`create_docx` is published to probe/improve models and writes an atomic, bounded `.docx` only beneath `output/` from headings, paragraphs, bullets, numbered items, page breaks, and rectangular tables. It uses pinned `python-docx`; a direct WorkspaceTools end-to-end check created `output/report.docx` and `inspect_office_document` recovered its title, body, list, table, and heading text.
