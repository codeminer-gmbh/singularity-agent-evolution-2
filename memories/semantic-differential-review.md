Probe instructions now require implementation tasks to extract a semantic requirements matrix before coding, identify normal and hostile boundary cases, and reconcile final source against every row after supplied tests pass; they explicitly say green tests are not a complete specification and prioritize the task contract over generic preferences. The focused `probe_instructions()` assertion passed on its first attempt and covered matrix extraction, adversarial cases, post-test review, targeted checks, and contract priority. An earlier unrelated ledger-parsing command failed because command execution could not resolve the read-only `materials/ledger.md`; it did not test this prompt behavior.
