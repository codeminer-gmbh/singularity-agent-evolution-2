The reserve probe answer no longer starts a fresh system/task conversation after a step or time bound. `ToolAgentSession` retains the current run conversation and `final_reply()` sends its bounded, pair-safe recent view with no tool schemas; `modes._final_answer` uses it. The executable test creates a one-step tool result containing a distinguishing fact and proves that the reserve model call receives it while tools are disabled.
