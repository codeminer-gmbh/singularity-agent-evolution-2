Probe and improvement instructions now require implementation and exact-output tasks to become executable acceptance checklists covering exact literals/bytes, boundaries, errors, side effects, and adversarial host-language hooks. They direct the agent to run focused behavioral checks, repair observed mismatches, and rerun the same oracle rather than relying on compile or happy-path smoke checks.
