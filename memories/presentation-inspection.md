`inspect_presentation` exposes a bounded, passive PPTX preview through the normal tool registry. It validates archive/source limits, follows presentation slide ordering, extracts text shapes and table cells, and resolves optional speaker notes only through each slide's internal OOXML relationship (external targets are ignored). It returns valid bounded JSON with metadata, slide count, and omission reporting.
