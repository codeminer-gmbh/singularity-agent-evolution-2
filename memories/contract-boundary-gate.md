The probe system prompt now requires a private contract checklist, preserves unspecified cases, calls out zero/empty/equality and inclusive boundaries, asks for a discriminating fixture for rule-like work, and requires final examples to agree with the policy. This addresses ledger rejections for boundary-policy answers that stated a rule but contradicted it in a zero/edge example.
