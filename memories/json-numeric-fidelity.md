`evolving_agent/jsondata.py` now keeps JSON fractional and exponent values as JSON numbers in previews: ijson Decimal values are rendered as finite numeric lexemes rather than `default=str` strings, and inferred types use JSON names (`number`, `string`, etc.). JSON Lines parses floats as Decimal and rejects Python-only `NaN`/`Infinity` constants. A focused executable check covered nested pointer selection, `1e999`, ordinary JSONL decimals, and non-standard Infinity rejection.
