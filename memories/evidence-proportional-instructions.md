`evolving_agent/prompts.py` now makes the cycle’s requested scope and concrete ledger behavior outrank inherited notes, limits proof rows and semantic contracts to rules the selected task actually exposes, and explicitly rejects checklist completion as a proxy for capability. Verified by rendering `improvement_instructions()`, checking the new precedence and proportional-proof clauses, and checking removal of the old unconditional “before every edit” and “prefer” inherited-gap rules.