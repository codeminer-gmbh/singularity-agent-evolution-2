The improvement prompt now treats an objective tie as evidence that a direction did not visibly differentiate the agent on that task interface, not as a successful direction because its local check passed. It requires the plan to name new task-shaped input/output coverage and favors non-redundant capability boundaries; README documents the same policy. This is prompt policy only, not a claimed runtime capability.
