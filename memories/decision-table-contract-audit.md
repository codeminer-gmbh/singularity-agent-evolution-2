Probe instructions now require a private decision table with concrete witnesses for each contract clause on parser, stateful-rule, algorithm, and calculation tasks. It explicitly covers equality endpoints, zero/empty modes, distinguished malformed forms, and specified error precedence, then requires executing or tracing those rows and reconciling final code/examples to them. This targets ledger losses where answers contradicted their stated behavior at zero/equality boundaries or over-rejected quote forms.
