The strict YAML inspector's JSON Pointer decoder now processes `~0` and `~1` exactly once from left to right. This preserves RFC 6901's valid `~01` encoding for a literal `~1` mapping key, which chained replacements previously treated as a newly introduced invalid escape. A focused published-tool check selected both a slash-containing key and that literal-tilde key, and confirmed a non-string pointer is rejected.
