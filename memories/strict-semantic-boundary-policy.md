`probe_instructions()` now directs the task-solving model to decide exact parsing boundaries, reject convenient syntax outside the stated language (including the ledger’s ASCII-digits-only `+7` counterexample), and verify the boundary through the public interaction without expanding this into a universal checklist. `tests/test_probe_semantic_boundaries.py` exercised a `ToolAgentSession` and inspected the system message actually sent to its model; the full suite passed (8 tests). The removed publication-freshness note was not retained because the ledger audit had found its claimed coverage inaccurate.
