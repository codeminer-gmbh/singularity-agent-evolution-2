`evolving_agent/prompts.py` now has the probe and improvement prompts model contract exceptions as execution order: an invalid payload behind an ignored discriminator must not be read, and unbounded closure traversal needs a deep valid worklist witness. It also makes the ledger-first opening order consistent with the scheduler’s ledger-first handoff. This follows ledger losses on ignored delete payloads and deep recursive closure handling after generic boundary tables were already added.
