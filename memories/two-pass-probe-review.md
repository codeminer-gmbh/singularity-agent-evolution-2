`ToolAgentSession` now gives probe drafts two distinct final-review turns when budget permits: first literal requirements traceability (including eager validation and boundary semantics), then an independent red-team operational/output pass (including repeated hot-path work and mutation behavior). The second pass was chosen after ledger verdicts repeatedly exposed omissions that a prior generic one-pass review did not catch.