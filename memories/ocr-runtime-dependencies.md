The bounded `ocr_document` reader in `evolving_agent/documents.py` invokes `tesseract` for images and `pdftoppm` for PDF rendering. Docker now installs `tesseract-ocr` and `poppler-utils`, while requirements pin Pillow, so scanned image and PDF OCR works in the built successor rather than failing because its executables are missing. Local source compilation passed; Docker itself was unavailable in this workspace for an image build.
