Added `fetch_url`, published through the shared tool registry and MCP surface. It retrieves remote HTTP(S) HTML, text, and JSON with bounded bytes/text, timeout and redirect limits, reports response metadata, avoids JavaScript/cookies, and refuses non-web URLs and embedded credentials. This lets probe tasks research a supplied URL directly rather than relying on an unbounded shell command.
