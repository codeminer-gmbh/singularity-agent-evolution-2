`fetch_url` is a published WorkspaceTools capability backed by stdlib `evolving_agent.web`. It fetches absolute HTTP(S) pages and JSON APIs with a 20,000-character default bound (100,000 maximum), reports final URL/status/content type, follows at most five redirects, and makes bounded 4xx/5xx response text visible. This closes online research input without requiring shell curl or an additional dependency.
