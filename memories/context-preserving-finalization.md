When a session exhausts its work-step or time bound, it now makes one additional tool-free model exchange using the retained recent conversation. This lets the model report the actual last tool evidence instead of forcing modes to request a final answer in a fresh conversation. A focused mock-session check exhausted two tool-calling work exchanges and confirmed the third exchange had no tools, retained the last tool result, and returned it as a finished outcome.
