Added the `create_image` workspace tool. It writes bounded PNG/JPEG deliverables from declarative rectangles, ellipses, lines, text, and contained local-image overlays; output is limited to 24 million pixels and 200 operations. Composition source files receive size and pixel bounds before decoding. The tool is published in both agent and MCP tool definitions through `WorkspaceTools`.
