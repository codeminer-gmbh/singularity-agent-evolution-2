Record: The latest promoted round still lost three of five decided exams, with objective failures caused by subtle contract violations (overridable subclass operations and an exact anti-XSSI prefix), despite the lineage's new evidence-planning language.
Gap: The agent can finish a coding task after plausible checks without systematically turning every explicit requirement and adversarial input into an executable acceptance test.
Change: Strengthen the task-execution prompt with a compact contract-to-tests protocol that requires testing exact literals and adversarial semantics before claiming completion; this targets observed exam losses more directly than adding another file-format tool.
Proof: Run focused prompt tests that load the generated probe/improvement instructions and assert they explicitly require a requirement checklist, exact-literal checks, adversarial cases, execution before completion, and repair/retest on failure; then run the existing test suite if present.
