Cannot: hidden boundary interactions still defeat delivered solutions even after happy-path checks; the ledger’s smallest fact is that a recent candidate passed only 9 of 34 checks, so wrong behavior remained observable while the public contract path was not tested against an independent oracle (distinguishing input: one chunked stream containing a malformed candidate followed by a valid frame).
Change: make probe completion require a runnable, task-derived adversarial oracle/differential check for the highest-risk stateful rule, with explicit malformed→valid recovery and competing-order expectations, and reserve an exchange to repair what that check exposes.
Why: repeated prompt-only completion reviews still lost executable tests, while new format tools and more generic gates are already well covered; forcing semantic counterexamples directly targets the costliest supported gap rather than repeating inaccurate verification claims.
