Verification evidence is now cryptographically bound to the candidate tree: `Workspace.digest(exclude=...)` hashes sorted non-skipped files with length-prefixed paths and content, and `verification_problems` compares `candidate_digest` with the current tree while excluding the receipt to avoid self-reference. The prompt tells future rounds to write this binding last. `tests/test_candidate_digest_binding.py` proves a receipt succeeds for its original tree and fails after a source edit.
