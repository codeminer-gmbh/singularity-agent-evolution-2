Added `inspect_office_document` to the shared tool registry for safe, bounded DOCX and PPTX evidence inspection. `evolving_agent/office_documents.py` reads only selected XML parts from a ZIP package, returns text previews and media metadata, rejects unsafe/unsupported packages, and never opens macros, links, embedded objects, or media payloads. Verified with synthetic DOCX text/media and PPTX slides (numeric slide order), plus `python -m compileall -q evolving_agent`.
