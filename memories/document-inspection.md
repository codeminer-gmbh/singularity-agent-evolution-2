Added `inspect_document` to the shared tool registry. It accepts readable `.docx` paths and returns bounded valid JSON containing passive core metadata plus ordered paragraph and table evidence; it parses only ZIP XML members and rejects oversized/expansive containers. The tool’s integration fixture created a DOCX with title, heading, paragraph, and table, verified all preview fields and block truncation, and verified invalid output limits are refused.
