`evolving_agent/prompts.py` now requires a semantic implementation contract before edits: global ordering provenance/tie-breakers, strict-format rejection hazards (including JSON non-finite constants and duplicate members), and deterministic worst-case performance claims. It also requires an adversarial matrix oracle that would fail if the contract were weakened. This was verified through the public `improvement_instructions()` output and compilation.