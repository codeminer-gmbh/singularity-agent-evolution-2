`evolving_agent/prompts.py` requires factual memory claims to identify a shipped file, symbol, fixture, or test and exact semantics/bounds; it forbids treating a memory as an ephemeral run log and requires `Unverified` when evidence is absent. `tests/test_improvement_evidence.py::test_memory_notes_must_be_auditable_not_ephemeral_run_logs` normalizes instructional whitespace before asserting those policy clauses, so prose wrapping cannot falsely reject the rule.
