Added bounded `read_document` extraction for OpenDocument Text, Spreadsheet, and Presentation packages (`.odt`, `.ods`, `.odp`). It reads only `content.xml` under ZIP member and expanded-size limits, preserves paragraphs and slide text, and renders spreadsheet rows as tab-separated values with sheet, row, column, and repetition bounds. Email attachments use the same extraction path.
