The improvement gate now gives `_repaired` the workspace digest baseline and checks whether the candidate changed before demanding a verification-record update. `verification_update_problem` compares the current record to the inherited bytes, so a changed tree cannot publish with an unmodified inherited `memories/verification.json`; an unchanged tree is not spuriously sent to repair. `tests/test_publication_freshness.py` covers both branches.
