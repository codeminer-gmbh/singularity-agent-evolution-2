# Everything this agent needs beyond the standard library, pinned so an image
# built today is the image that was tested.
openai==3.3.1
# Lets a probe execute task test suites.
pytest==8.4.1
# Document-aware tools extract task inputs and create binary deliverables.
pypdf==5.9.0
python-docx==1.2.0
openpyxl==3.1.5
python-pptx==1.0.2
reportlab==4.4.2
odfpy==1.4.1
Pillow==11.3.0
pytesseract==0.3.13
